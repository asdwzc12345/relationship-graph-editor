# 更新记录

## 4.3.2 - 2026-08-09

- 只读 HTML 顶部新增主题选择，可在“跟随系统、浅色、深色”之间即时切换，默认跟随电脑主题。
- 深色主题同步覆盖页面、工具栏、网格画布、分组、节点、文字、关系名称背景和操作提示。
- 节点颜色分类在深色主题下仍保持蓝、绿、粉、紫、橙等辨识差异，关系线继续采用起点颜色。
- 通过真实浏览器验证浅色和深色下的页面背景、节点填充及系统颜色模式均正确变化，页面无脚本错误。

## 4.3.1 - 2026-08-09

- 只读 HTML 的鼠标规则与主程序统一：左键点击节点、分组或关系执行选择及关联高亮。
- 右键单击画布或对象会清除当前全部选择与高亮，右键拖动用于平移整个画布。
- 左键拖动不再平移视野，避免选择对象时因轻微移动造成画布偏移。
- 右键拖动期间显示十字光标，浏览器原生右键菜单继续保持禁用。
- 通过真实浏览器验证左键选择、右键清除和左键拖动不平移，页面无脚本错误。

## 4.3.0 - 2026-08-09

- 只读 HTML 升级为离线交互查看器，顶部新增“显示连线”开关，可即时隐藏或恢复全部关系线。
- 点击节点会按完全同名节点合并展示相关关系；点击分组或关系也会高亮对应对象，其余内容自动变暗。
- 只读查看器支持双向、上游、下游和 1～3 层关系查看，显示规则与主程序保持一致。
- 支持按住左键拖动画布平移、鼠标滚轮缩放、按钮放大缩小以及一键恢复适合窗口。
- 所有交互仅改变当前浏览视野和高亮状态，不写回关系图；HTML 仍是单文件、离线可用并可重新导入主程序。
- 保持禁用浏览器右键菜单，并通过真实浏览器验证连线开关、节点响应、拖动和缩放，页面无脚本错误。

## 4.2.2 - 2026-08-09

- 方向过滤现在也作用于实际点击节点：上游模式无入线或下游模式无出线时，该节点同样显示为暗色。
- 暗色的实际点击节点仍保留选中边框和唯一逻辑选中状态，属性编辑、移动、删除以及邻居计算均不受影响。
- 更新回归测试，验证实际点击节点可以同时处于“逻辑选中”和“方向暗化”状态。

## 4.2.1 - 2026-08-09

- 同名节点联动高亮增加方向过滤：上游模式下，无入线的同名副本不再点亮；下游模式下，无出线的同名副本不再点亮。
- 实际点击的节点始终保留真实选中标识；双向模式继续点亮全部完全同名节点。
- 该过滤仅影响画布显示，不改变逻辑选择集合、同名根节点集合、邻居遍历方向或层数计算。
- 增加无方向连线同名节点的回归测试，并验证被视觉过滤的节点仍保留在内部计算集合中。

## 4.2.0 - 2026-08-09

- 框选或选中节点、分组后点击“＋分组”，会按照全部选中对象的整体边界创建合适大小的新分组。
- 新分组顶部自动预留标题区域，左右和底部保留统一边距，并设置合理的最小尺寸。
- 选中内容含已有分组时，新分组自动成为其外层父分组；选中节点和子分组的归属关系会即时更新。
- 没有选中节点或分组时，继续在当前屏幕中心创建默认大小的空分组。
- 增加混合框选包组、自动嵌套、排除未选节点以及无选择居中新增的回归测试。

## 4.1.0 - 2026-08-09

- 单击一个节点时，全图所有名称逐字完全相同的节点同步显示蓝色高亮。
- 同名节点只进行视觉联合，逻辑选择集合仍仅包含实际点击节点；移动、删除、复制和属性编辑不会误作用于其他同名节点。
- 邻居计算将全部同名节点作为共同起点并合并结果，同时继续遵循上游、下游、双向以及 1～3 层深度规则。
- 相似但不完全一致的名称不会联合高亮；名称比较采用严格的逐字匹配。
- 框选形成的真正多选仍不自动显示邻居，不受同名联合高亮影响。

## 4.0.1 - 2026-08-09

- 分组绘制顺序改为按面积从大到小：较大的外层分组始终先绘制并位于底层，较小的内层分组显示在其上。
- 节点继续在所有分组和连线之后绘制，确保始终位于最上层，不会被分组背景遮挡。
- 点击重叠位置时优先命中节点；没有节点时优先命中面积更小、视觉层级更高的内层分组。
- 绘制和点击顺序不再依赖 JSON 中的分组排列，即使内层分组写在外层分组之前也能正确显示。

## 4.0.0 - 2026-08-09

- 数据格式升级至第 3 版：节点和分组均保存自动计算的多重分组归属，同时继续兼容旧版单一 `group` 字段和旧 JSON 文件。
- 节点可同时属于所有包含其中心位置的分组；嵌套结构下会得到完整的外层到内层归属链。
- 分组完整位于其他分组内部时自动成为子分组，支持多层嵌套和多个独立包含关系。
- 右侧节点与分组属性移除可编辑的所属分组下拉框，改为只读层级路径；独立重叠分组会分行显示，不会伪造父子关系。
- 移动节点、分组或改变分组大小后立即重新匹配归属；移动外层分组会同步移动全部子分组和内部节点。
- 混合复制粘贴格式记录明确的节点和分组选择清单，正确处理节点与其所属分组同时被框选的情况，并继续兼容旧剪贴板数据。
- 飞书画板导出保留主要分组嵌套，子分组和节点在导入后仍位于对应父分组中。

## 3.8.1 - 2026-08-09

- 新关系完成时不再沿用拖动过程中的临时方向，而是根据起点和目标的最终相对位置重新选择两端连接方向。
- 上下排列且横向重叠的对象固定使用“上方对象下边缘 → 下方对象上边缘”；左右排列且纵向重叠时使用相向的左右边缘。
- 打开、移动或缩放对象时会自动修复已有关系的错误起点和终点方向。
- 连线预览与最终结果采用同一方向判断，避免松开鼠标后线路突然换边。

## 3.8.0 - 2026-08-09

- 撤销和重做不再重新执行“适合窗口”，会保持操作前的画布缩放比例与视野位置。
- 框选扩展为节点与分组混合选择；完整位于选择框内的分组会与框内节点同时选中。
- 混合选择支持整体移动、复制、粘贴和删除，右侧属性栏显示选中的节点与分组数量。
- 节点和分组多选状态下不会自动高亮邻居；单个分组选中时仍可使用边缘和四角缩放。

## 3.7.0 - 2026-08-09

- 分组选中后也不再显示四个连线圆圈，并移除不可见的分组端口点击区域。
- 选中分组后，四条边均可直接拖动缩放；鼠标会显示水平或垂直缩放光标。
- 分组四角继续支持双向缩放，并显示对应的斜向缩放光标。
- 拖动未选中的分组仍会自动按拖动方向选择端口并创建关系；拖动已选中分组内部仍用于移动。

## 3.6.1 - 2026-08-09

- 节点选中后不再显示上、右、下、左四个连线圆圈，并同步移除节点上不可见的端口点击区域。
- 节点仍支持四方向连线：拖动未选中的节点时，根据拖动方向自动选择对应端口；选中节点拖动始终用于移动。
- 分组的四个连线圆圈和四角缩放控制点保持不变。

## 3.6.0 - 2026-08-09

- 点击工具栏或菜单中的“新增节点”“新增分组”时，对象改为创建在当前画布视野中心，不再使用固定的初始排列位置。
- 新分组以自身中心对准屏幕中心；新节点同样精确居中，若该位置位于分组内则自动归入该分组。
- 画布平移或缩放后再次新增，会实时跟随新的屏幕中心。
- 新增节点、分组居中创建、自动归组及平移后中心更新回归测试。

## 3.5.0 - 2026-08-09

- 粘贴节点、分组及其内部关系时，不再按原位置向右下方固定偏移，而是将所选内容的整体中心放到当前画布视野中心。
- 粘贴位置会实时考虑画布平移、缩放及属性栏占用的实际可见区域；移动视野后再次粘贴会落到新的屏幕中心。
- 新增多节点粘贴中心位置回归测试，并保留粘贴后的多选状态和内部关系复制规则。

## 3.4.0 - 2026-08-09

- 新增“飞书画板（draw.io，可编辑）”导出，生成飞书新版画板可直接导入的 `.drawio` 文件。
- 节点、分组和连线均导出为独立且未锁定的图形对象；导入飞书后每个节点可单独移动和编辑，不会合并成图片。
- 保留分组归属、节点位置和大小、节点名称与类型、关系名称、线型、连接方向及跟随起点的线色。
- 新增 draw.io XML 合法性、独立节点、分组容器和分组连线回归测试。

## 3.3.0 - 2026-08-09

- 对齐吸附扩展到节点和分组之间：节点可参考分组，分组也可参考节点或其他分组。
- 等间距吸附支持节点、分组任意混合排列，并继续显示对齐参考线、等距测量线和间距数值。
- 拖动分组时同步显示吸附提示；按住 `Ctrl` 拖动分组可关闭网格、对齐和等间距吸附，自由摆放。
- 新增节点对齐分组、分组混合等间距及分组自由拖动回归测试。

## 3.2.0 - 2026-08-09

- 画布改为理论无限，可将节点和分组放到原点左上方或任意远处，不再显示或使用固定画布边界。
- 节点移动、分组移动与缩放、双击创建、复制粘贴和数据恢复均不再按旧画布尺寸裁剪位置。
- “适合窗口”按当前全部节点、分组及连线路径自动取景；对跨度较大的关系图支持更低缩放比例。
- PNG、PDF、SVG 和只读 HTML 导出按实际内容范围生成，负坐标及远处内容不会被裁掉。

## 3.1.0 - 2026-08-09

- 支持双击直线、折线或曲线，直接在画布中修改关系名称。
- 关系名称输入框显示在实际线路径中点；按 `Enter` 或点击别处保存，按 `Esc` 取消。
- 关系名称仍为可选项，可通过双击后清空输入内容删除名称。
- 新增大幅弯曲曲线的双击命中、名称保存及清空回归测试。

## 3.0.0 - 2026-08-09

- 关系线颜色改为实时跟随起点节点的颜色分类，不再由关系类别的颜色决定。
- 修改起点节点颜色后，所有从该节点发出的已有线条和箭头立即同步；终点改色不会影响入线。
- 分组作为起点时使用分组边框色；关系类别继续作为语义分类保留，并在属性中明确线色跟随起点。
- SVG、PNG、PDF 和只读分享图同步使用起点颜色。
- 新增右侧属性改色、已有线条同步、终点改色隔离及 SVG 线条/箭头颜色回归测试。

## 2.9.3 - 2026-08-09

- 修复曲线名称按两个节点中心的中点放置，导致名称与实际曲线路径距离过远的问题。
- 直线、折线和曲线名称统一按实际路径长度的 50% 位置放置，并保留紧凑背景保证可读性。
- SVG、PNG、PDF 和只读分享图同步采用实际路径中点，避免导出后名称再次偏移。
- 新增大幅弯曲曲线的名称位置及主画布/导出一致性回归测试。

## 2.9.2 - 2026-08-09

- 修复横向拉线时目标节点可能错误连接到上方或下方，而不是面向源节点一侧的问题。
- 入口选择改为比较上、右、下、左四个真实连接点到源端口的距离，不再比较矩形边所在的无限直线。
- 加载关系图以及移动节点或分组后，会自动修复旧数据中不合理的目标入口。
- 新增左向右、右向左真实拉线和旧入口修复回归测试。

## 2.9.1 - 2026-08-09

- 修复拉线到第 4 个或更后面的节点/分组后，右侧起点或终点下拉框可能因 `SelectedIndex` 越界导致程序报错的问题。
- 端点下拉框改为先加入属性面板并完整填充，再在有效范围内设置选中项，不再依赖延迟数据绑定。
- 新增从第 1 个节点真实拖线到第 4 个节点，并打开新关系属性的完整回归测试。

## 2.9.0 - 2026-08-09

- 主程序取消“查看 / 编辑”模式切换，删除工具栏中的两个切换按钮。
- 启动、新建空白图以及导入 JSON 或只读可视图后，默认且始终处于编辑状态。
- 右侧属性从程序打开起即可直接修改，画布编辑、框选、拖动和快捷键无需切换模式。
- 只读 HTML 仍作为独立分享格式保留，重新导入主程序后可继续编辑。

## 2.8.0 - 2026-08-09

- 新增水平和垂直等间距吸附，支持节点位于两个节点之间，以及位于一排节点最前或最后的布局。
- 接近等间距位置时自动吸附，并显示两段同长度测量线和“等距”数值提示。
- 等间距识别仅比较同一行或同一列附近节点，减少跨区域误吸附。
- 按住 `Ctrl` 自由拖动时，同时关闭对齐、网格及等间距吸附和提示。

## 2.7.2 - 2026-08-09

- 按住 `Ctrl` 并用鼠标左键拖动节点时，临时关闭节点自动对齐和 5 单位网格吸附，可自由摆放。
- 自由摆放期间不显示横向或纵向对齐参考线，松开 `Ctrl` 后普通拖动自动恢复吸附。
- 区分 `Ctrl+单击` 与 `Ctrl+拖动`，继续保留多选切换，同时避免拖动前误取消节点选择。

## 2.7.1 - 2026-08-09

- 拖动单个或多个节点时，节点边缘和中心会自动吸附到其他节点的对应边缘或中心。
- 吸附距离随画布缩放自动调整，在不同缩放比例下保持一致的操作手感。
- 拖动过程中显示横向、纵向虚线参考线，清楚标识当前对齐位置。

## 2.7.0 - 2026-08-09

- 编辑模式下在画布空白处按住鼠标左键拖动即可框选节点，不再要求按住 `Shift`。
- 框选或粘贴后同时选中多个节点时，只标识选中内容，不再自动高亮邻居或淡化其他对象。
- 新增 `Ctrl+C` / `Ctrl+V` 复制粘贴：支持单个或多个节点，以及包含组内节点的完整分组。
- 复制节点只携带所选节点之间的内部关系；复制分组携带分组、组内节点和内部关系，不误带外部关联。
- 粘贴内容使用新标识符并整体错开，粘贴结果保持选中且可通过撤销恢复。

## 2.6.1 - 2026-08-09

- 颜色分类仅显示灰蓝色、蓝色、绿色、粉色、紫色、橙色，不再显示右侧括号用途说明。
- 保留每个颜色选项左侧的真实色块。

## 2.6.0 - 2026-08-09

- 删除右侧属性面板中的“应用修改”和“批量应用”按钮。
- 编辑模式下文本输入立即反映到画布，下拉选择立即生效，并在短暂停顿或失焦时自动保存。
- 连续输入合并为一次撤销记录，避免逐字产生大量历史记录。
- 颜色分类改为原生色块选项，并显示明确颜色文字。
- 查看模式仍保持严格只读。
- 新增无应用按钮、属性自动应用、颜色色块与文字示例自检。

## 2.5.4 - 2026-08-09

- 查看模式下右侧属性面板改为严格只读，节点属性不能修改。
- 只读限制同时覆盖图名称、分组和关系属性；文本仍可选择和复制。
- 切换到编辑模式后输入框、下拉框和应用按钮恢复可编辑，返回查看模式后再次锁定。
- 新增查看、编辑、返回查看三段式属性权限回归测试。

## 2.5.3 - 2026-08-09

- 将节点属性中的“外观”改名为更明确的“颜色分类”。
- 增加说明：颜色分类仅影响节点配色，不影响节点类型、关系或功能。

## 2.5.2 - 2026-08-09

- 修复在右侧属性输入框按 Backspace 修改文字时，选中节点被误删除的问题。
- Backspace 不再绑定删除对象，只保留正常的文字回退功能。
- Delete 仅在画布未聚焦任何输入框、下拉框或数值输入控件时删除对象。
- 新增 Backspace、输入状态 Delete 和画布 Delete 三种键盘行为自检。

## 2.5.1 - 2026-08-09

- 修复双击空白处新建节点后，再双击节点名称完成编辑时节点可能消失的问题。
- 取消未发生移动的鼠标手势时不再用快照替换整份关系图，避免新节点引用失效。
- 进入画布内编辑前会重新从当前关系图获取节点或分组。
- 新增“真实双击创建节点 → 双击名称 → 确定修改 → 节点仍存在”的完整回归测试。

## 2.5.0 - 2026-08-09

- EXE 文件图标、任务栏图标、主窗口左上角图标统一为 `assets/app-icon.ico`。
- 原生新增关系弹窗也使用同一程序图标。
- 图标以 Windows 可执行文件资源和托管资源两种方式嵌入，分享单个 EXE 时无需附带图标文件。
- 新增主窗口图标加载和 EXE 内嵌图标资源自检。

## 2.4.0 - 2026-08-09

- 双击节点左上角类型角标可直接编辑节点类型。
- 双击节点中央名称可直接编辑节点名称。
- 双击分组左上角标题可直接编辑分组名称。
- 画布内编辑支持 Enter 或失焦保存、Esc 取消，并纳入撤销与自动保存。
- 节点文字和分组标题区域不再被识别为空白双击区域。
- 新增三处命中区域和直接编辑保存自检。

## 2.3.0 - 2026-08-09

- 支持在画布任意位置按住右键拖动，以平移整个关系图视图。
- 右键拖动期间鼠标显示十字光标，松开后恢复默认光标。
- 保留右键单击清除全部选择；只有未发生拖动时才执行清除。
- 新增右键拖动距离和光标状态自检。

## 2.2.0 - 2026-08-09

- 移除工具栏中的“＋关系”和“删除”按钮，相关功能仍可通过编辑菜单和快捷键使用。
- 编辑模式下双击画布空白位置可直接创建新节点，并以双击位置为节点中心。
- 双击位置处于分组范围内时，新节点会自动归入该分组。
- 新增双击空白事件、节点生成位置和自动归组自检。

## 2.1.0 - 2026-08-09

- 新增跟随系统、浅色和深色三档界面主题，默认读取 Windows 应用颜色模式。
- 深色主题覆盖窗口标题栏、菜单、工具栏、状态栏、属性面板、输入控件和原生关系图画布。
- 系统主题变化时，处于“跟随系统”模式的窗口会自动刷新。
- 主题偏好保存在本机用户目录，对所有关系图持续生效。
- 新增深浅主题切换和深色画布输出自检。

## 2.0.1 - 2026-08-09

- 修复主窗口创建早期设置分栏最小宽度和分隔位置时，因容器尚未完成布局而导致程序无法启动的问题。
- 新增完整主窗口构造与分栏范围自检，后续构建会直接拦截同类启动错误。

## 2.0.0 - 2026-08-09

- 使用 C#、WinForms 与 GDI+ 完整重写桌面程序，运行时不再启动浏览器、本地网页服务或加载 HTML/JavaScript。
- 以标准 Windows 菜单、工具栏、状态栏、属性面板和原生画布替换原网页界面。
- 保留并重新实现节点、分组、关系编辑，支持四方向连线、三种线型、多选、移动、分组缩放和上下游高亮。
- 修复高连接节点的邻居高亮逻辑，关系只按实际端点遍历，不再将节点的分组归属误当作关系。
- 节点允许未分组，分组允许作为关系端点；关系名称允许留空，删除无需二次确认且可撤销。
- 保留 JSON 与只读 HTML 可视图导入，并支持导出 JSON、只读 HTML、SVG、PNG 和 PDF。
- 生成可单独复制分享的单文件 `关系图编辑器.exe`，默认图《测试用图》直接嵌入程序。
- 新增原生独立运行自检和构建审计。

## 1.3.9 - 2026-08-09

### 禁用浏览器内核右键手势

- 桌面窗口优先使用 Chrome，避免 Edge 内置右键轨迹触发刷新、前进或后退。
- 为桌面窗口使用独立浏览器配置目录，保证禁用参数不会被已有浏览器进程忽略。
- 禁用桌面窗口扩展，并补充 Edge 鼠标手势功能开关作为回退保护。

## 1.3.8 - 2026-08-09

### 只读关系图禁用右键手势

- 只读分享版全页面禁用浏览器原生右键菜单和右键辅助点击。
- 画布平移仅响应主鼠标左键，右键不再触发拖动画布。
- 增加产品检查，防止后续打包时遗漏只读分享版的右键限制。

## 1.3.7 - 2026-08-09

### 全面稳定性与性能检查

- 节点拖动、分组缩放和框选收到系统取消事件时会完整回滚，不再保存半截位置或产生意外选择；切换查看/编辑模式也会安全结束未完成的交互。
- 导入关系图或恢复默认图时会清除旧布线路径缓存，避免相同 ID 的新节点沿用旧位置的连线。
- 重叠分组区域按视觉层级识别最上层分组；分组名称区域现在与分组主体一样支持选择、移动和拉线。
- 节点、分组、关系筛选及上下游聚焦改为索引查找，避免大图绘制和搜索时反复扫描全部数据；只读分享版同步优化。
- 项目数据库可用时，本地兼容镜像写满不再误报为项目保存失败；保存中和保存完成状态更加准确。
- PNG 与 PDF 导出除最长边限制外增加 3200 万总像素上限，降低超大画布耗尽内存的风险。
- 桌面本地服务增加内容安全策略、控制端点请求方法和网页来源校验，并放宽后台心跳容忍时间，兼顾防外部网页干扰与最小化窗口稳定性。

## 1.3.6 - 2026-08-09

### 聚焦高亮与防误连

- 点击节点或分组后，除了关系线外，直接与其相连的邻居节点和分组也会显示明确的描边高亮；只读分享版保持一致。
- 多选状态下单击其中一个节点会收敛为单选并显示其邻居；直接拖动仍会移动整个多选集合。
- 未选节点或分组只有拖动超过 7 像素才会创建关系，普通点击只负责选中，避免误连到下方的所属分组或重叠节点。
- 快速拖线仍会按最终指针位置识别方向和目标，不影响正常的新建关系操作。

## 1.3.5 - 2026-08-09

### 桌面操作限制

- 全界面禁用浏览器原生右键菜单，同时保留画布右键取消全部选择。
- 禁用右键鼠标手势、中键自动滚动、鼠标侧键前进/后退和触控板历史导航。
- 禁用 Ctrl/Command + 滚轮页面缩放、刷新、查看源码、开发者工具等网页快捷操作。
- 禁止把文件或页面内容拖入软件触发浏览器打开或导航。
- 桌面启动器增加禁用缩放、历史滑动与 Edge 鼠标手势的启动参数。

## 1.3.4 - 2026-08-08

### 分组拖动规则

- 分组与节点采用一致的主体拖动规则：未选中时拖动为拉线，选中后拖动为移动。
- 移动分组时，属于该分组的节点会保持相对位置并随分组一起移动。
- 选中分组后仍可使用四边与四角控制点调整大小，四向圆点继续用于指定方向拉线。
- 分组移动过程实时更新以分组或组内节点为端点的关系线，并支持撤销。

## 1.3.3 - 2026-08-08

### 分组关系端点

- 分组可作为关系的起点或终点，支持“分组 → 节点”“节点 → 分组”和“分组 → 分组”。
- 选中分组后显示上、右、下、左四个连线圆点，可直接拖到节点或其他分组。
- 新增关系与关系编辑窗口同时列出节点和分组，并明确标注端点类型。
- 查看模式、搜索、上下游聚焦、关系详情、复制粘贴、JSON 及只读 HTML 均保留分组关系。
- 删除分组时会同步删除以该分组为端点的关系，组内节点仍保留并转为未分组。

## 1.3.2 - 2026-08-08

### 可选分组

- 节点的所属分组改为可选，单个和批量编辑均可设为“未分组”。
- 项目允许没有任何分组，新建空白项目不再自动生成默认分组。
- 最后一个分组可以直接删除；原组内节点保留位置并自动变为未分组。
- 未分组节点支持新增、移动、复制粘贴、自动排版、JSON 与只读 HTML 导入导出。

## 1.3.1 - 2026-08-08

### 只读版重新导入

- “导入文件”现在同时接受 JSON 和本工具导出的只读交互版 HTML。
- 只读 HTML 中的关系图数据会安全提取，不会执行导入文件包含的脚本。
- 普通网页和缺少关系图数据的 HTML 会被拒绝，并显示明确错误。
- 新增“导出只读版 → 重新导入”的无损往返测试，并将解析核心嵌入单文件 EXE。

## 1.3.0 - 2026-08-08

### 单文件便携版

- 将界面、样式、脚本、默认关系图和品牌图标全部嵌入 `关系图编辑器.exe`。
- EXE 不再读取同目录的 `index.html` 或其他项目文件，可单独复制、发送和运行。
- 桌面运行测试改为把 EXE 复制到空临时目录后启动，并逐项验证内嵌页面、样式、脚本、默认图和图标。
- 继续复用电脑已有的 Microsoft Edge 或 Google Chrome，无需安装额外运行库。

## 1.2.0 - 2026-08-07

### 节点交互

- 节点新增上、右、下、左四个关系连接点，拖线创建的关系会保留所选出线方向。
- 编辑模式中，拖动已选节点执行移动，拖动未选节点直接创建关系。
- 画布支持右键取消当前全部选择。
- 编辑界面右侧新增“直线、折线、曲线”选择，只作用于之后手动新建的关系线。
- 点击节点触发的自动聚焦布线保持独立，不读取右侧的新建线型选择。
- 关系名称改为可选；留空时不再生成占位名称或显示空标签。
- 节点、分组、关系和项目删除改为立即执行，不再弹出二次确认。

### 默认演示内容

- 默认项目改为“测试用图”，以 4 个分组、12 个节点和 16 条关系展示一条简洁、清晰的工作闭环。
- 示例关系覆盖需求输入、方案执行、质量检查、结果交付、反馈收集和下一轮改进。

### 桌面视觉重构

- 新增渐变品牌图标与 Relationship Studio 品牌识别，统一浅色和深色视觉系统。
- 全局搜索进入主顶栏；撤销、重做、导入和导出形成清晰的高频操作区。
- 新建、复制、恢复、设置、帮助和危险操作收进带说明的项目菜单，减少工具栏拥挤。
- 画布、详情栏、小地图、菜单、弹窗和通知改为独立桌面面板，增加层次、间距和交互反馈。
- 编辑模式继续使用独立操作行，1280 像素桌面窗口中所有工具完整可见且无横向溢出。

### Windows 桌面入口

- 新增可直接双击的 `关系图编辑器.exe`，不再以 `index.html` 作为推荐入口。
- 启动器使用本机 Edge 或 Chrome 的独立应用窗口，不显示地址栏和标签页。
- 内置仅绑定 `127.0.0.1` 的轻量静态服务，使用固定本地地址保存项目数据，并通过页面心跳自动退出。
- 新增 Windows 品牌图标、可复现构建脚本和桌面运行链路测试。

## 1.1.0 - 2026-08-07

### 数据安全

- 恢复历史版本前会在同一数据库事务中保存真正的当前版本。
- 重命名、读取恢复记录和执行恢复会等待尚未完成的项目保存，避免旧数据竞争。
- 新增 IndexedDB 恢复往返回归测试。

### 电脑端体验

- 将五种导出格式合并为单一导出菜单，修复 1280 像素窗口下的顶部操作裁切。
- 编辑操作使用独立第二行，并将排版和图层控制收进菜单；1024 与 1280 像素电脑窗口均可完整访问。
- 移除与单一关系类型完全重复的筛选预设。

### 性能与可靠性

- 平移和滚轮缩放改为动画帧内的轻量视窗更新，不再完整重建 SVG。
- 最近 8 组聚焦布线结果会被复用。
- JSON 导入增加文件大小、实体数量、画布范围、ID 和文本长度防护。

### 键盘与无障碍

- 新增关系支持按钮、`R` 快捷键和完整表单。
- 搜索结果提供组合框、活动选项和自动完成语义。
- 交互画布允许辅助技术识别内部节点、分组和关系控件。
